[project]
name = "academy-core"
version = "0.1.0"
description = "AI Academy chain runtime: roles, HITL gate, ledger, KPI rollup"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/academy_core"]
